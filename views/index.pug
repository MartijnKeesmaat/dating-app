extends layout

block content
  //- a( href='/icebreaker') Fill in form

  each user in users
    a(href='/users/' + user.id)
      h1= user.name
    
    p= `@${user.username}`
    ul
      li= user.website
      li= user.email
